---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: clamav-debian-sid
    image: debian:sid
provisioner:
  name: ansible
scenario:
  name: debian-sid
