---
# vars file for clamav
clamav_packages:
  - clamav
  - clamav-scanner
  - clamav-update

clamav_config_dir: /etc
clamav_config_file: clamd.conf

clamav_service: clamd

clamav_permission:
  - owner: clam
    group: clam
