---
# vars file for clamav
clamav_packages:
  - clamav
  - clamav-scanner
  - clamav-scanner-systemd
  - clamav-update

clamav_freshclam_log_dir: /var/log

clamav_service: clamd@scan

clamav_owner: clamscan
clamav_group: clamscan
